{# Framing line for the tail envelope. Renders at both guidance levels: it is
   provenance, not steering, and without it the block reads as user text. #}
Runtime context for you, not a message from the user. Do not mention it unless it bears on the answer.
